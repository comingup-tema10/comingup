---

---

<form>
    <input type="text" name="mail" value="  Skriv din mail">
  </form>
  <input type="submit"  class="submit" value="INDSEND">
<style>

  input {
    font-family: 'PT Sans', sans-serif;
    font-size: var(--fontsize-button-mobile);
    border: none;
    font-weight: 400;
    width: 20rem;
    height: 3rem;
    border-radius: 2rem;
    margin-top: 1rem;
    margin-left: 1rem;
    color:white;
    background-color: var(--accent-color1)
  }

  .submit{
    font-family: "PT Sans", sans-serif;
    font-size: var(--fontsize-button-mobile);
    border: none;
    font-weight: 700;
    width: 9rem;
    height: 2rem;
    border-radius:2rem;
    margin-top:1rem;
    margin-left:1rem;
    background-color: #f2f1eb;
    border: 2px solid var(--primary-color2);
    color: var(--primary-color2);
  }

  .submit:hover{
    background-color: var(--primary-color1);
    color:white;
  }

  @media (min-width:750px) {
    
    .submit{
        font-size:var(--fontsize-button);
        margin-left:2rem;
        margin-top:1.5rem;
        height:3rem;
        width:13rem;
    }

    input{
        width: 30rem;
        height: 3.5rem;
    }

  }

</style>
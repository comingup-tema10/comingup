<section class="hero">
  <h1>Mærk rytmen <br> på provinsen</h1>
  <div class="hero-content">
    <button class="cta-button">Se Events</button>
  </div>
  <h3 class="heading-three">COMING UP HOLBÆK</h3>
  <img src="/images/index/heroimg.webp" alt="Hero Image of young musicians" class="hero-image">
</section>
<div class="text-container">
    <p>Coming Up er en frivillig musikorganisation dedikeret til at fremhæve unge,  spirende sangere og musikere. <br> Vi giver dem en scene til at udfolde deres talent og bringe original, rå og autentisk musik til publikum. <br> Sammen skaber vi koncertoplevelser, der lyser op som et prisme og spreder kreativitet.</p>
</div>



<style>
/* Mobile-first Base Styles */
.hero {
  position: relative;
  width: 100%;
  height: 100vh;
  overflow: hidden;
  display: flex;
  flex-direction: column; /* Stack content vertically for mobile */
  align-items: center;
  text-align: center;
  margin-top: 2rem;
}

.hero-image {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  z-index: 1; /* Behind the text */
}

.hero h1 {
  position: relative;
  z-index: 2; /* Above the image */
  font-size: 2rem; /* Smaller font size for mobile */
  font-weight: bold;
  text-transform: uppercase;
  color:  #902d7b;
  line-height: 1.2;
  margin-top: 10px ;
  margin-left: 20px;
}

.hero-content {
  position: relative;
  z-index: 2;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 1rem;
  /* margin-top: auto;  */
  margin-top: -5px;
 
  
}

.cta-button {
  background-color: #f476fb;
  border: none;
  color: white;
  padding: 0.6rem 1.5rem;
  font-size: 1rem;
  border-radius: 5px;
  cursor: pointer;
  transition: background-color 0.3s ease, transform 0.2s ease;
  margin-left: 28px;
  
}

.cta-button:hover {
  background-color: #f0aaf4;
  transform: scale(1.05);
}

.heading-three {
    font-size: 1.5rem;
    font-weight: normal;
    color: white;
    position: absolute; /* Position relative to the hero section */
    bottom: 5%; /* Adjust as needed */
    left: 5%; /* Center horizontally */
    transform: translateX(-50%); /* Ensure perfect horizontal centering */
    z-index: 2; /* Ensure it appears above the hero image */
      word-spacing: 0.5rem;
}
.text-container{
    text-align: center;
    margin-top: 2rem;
    margin-bottom: 2rem;
    margin-right: 10px;
     margin-left: 10px;

}


/* Responsive Styles for Larger Screens */
@media (min-width: 768px) {
  .hero {
    flex-direction: unset; /* Switch back to centered layout */
  }

  .hero h1 {
    font-size: 3rem; /* Larger font size for tablets and desktops */
    top: 20%;
    position: absolute; /* Reposition text for larger screens */
  }

  .hero-content {
    position: absolute;
    bottom: 20%;
    flex-direction: column;
    gap: 1rem;
  }

  .cta-button {
    font-size: 1.2rem; /* Increase button size for larger screens */
    padding: 0.8rem 2rem;
  }

  .hero h3 {
    font-size: 1.8rem; /* Larger subheading for desktops */
  }
}

@media (min-width: 1024px) {
  .hero h1 {
    font-size: 4rem; /* Even larger font size for desktops */
  }

  .cta-button {
    font-size: 1.3rem;
  }
}




</style>